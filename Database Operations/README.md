I need these operations.
May have missed on progress at JB, 12-4-26, because I hadn't the database on my laptop.
So, prepare these, both for me and Assaf the tester:

(Requires connection to localhost:3306, at a RUNNING MySQL Server instance. Right?)
*   Create "vacation-wasteland" database with empty tables "users", "vacations" and "likes".
*   Create "vacation-wasteland" database with populated tables "users", "vacations" and "likes". The records are basic.